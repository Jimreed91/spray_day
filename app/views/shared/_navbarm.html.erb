  <nav class="navbar navbar-expand-md navbar-dark sticky-bottom dropup bg-success d-sm-none " >
  <div class="container-fluid text-center">
    <a class="navbar-brand" href="#">Spray Day</a>

    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <%= link_to "Home", "/", class: "nav-link active border-bottom border-top border-light" %>
        </li>
        <li class="nav-item dropdown">
          <% if current_user %>
            <%# <%= link_to "Farm", farm_path(current_user.farm), class: "nav-link active" %>
            <a class="nav-link active dropdown-toggle border-bottom border-light" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
              Farm </a>
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                  <li><%= link_to "Crops", crops_url, class: "dropdown-item" %></li>
                  <li><%= link_to "Products", products_url, class: "dropdown-item" %></li>
                  <li><%= link_to "Sprayers", sprayers_url, class: "dropdown-item"%></li>
                </ul>
          <% end %>
        </li>
        <li class="nav-item">
          <% if current_user %>
            <%= link_to "Spray Management", spray_programs_path, class: "nav-link active border-bottom border-light" %>
          <% end %>
        </li>
        <li class="nav-item">
          <%# <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a> %>
        </li>
      </ul>
    </div>

    <%# <p class="navbar-text float-right"> Logged in as <strong><%= user_signed_in? ? current_user.email : "...Nobody actually" %>

      <div class="nav-item dropdown align-">
        <%= image_tag "defbg.png", class: 'avatar dropdown-toggle', id: 'navbarDropdown', 'data-bs-toggle': 'dropdown', 'aria-haspopup': 'true', 'aria-expanded': 'false' %>
        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <%= link_to "Sign out", destroy_user_session_path, class:'dropdown-item', method: :delete %>
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'dropdown-item' %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'dropdown-item'  %>
            <%= link_to "Login", new_user_session_path, :class => 'dropdown-item'  %>
          <% end %>
        </div>
      </div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  </div>
</nav>
