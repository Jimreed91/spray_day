
<div class="nested-fields input-group">
  <div class="field">
    <%= f.collection_select :product_id, Product.all.where(farm: current_user.farm), :id, :name, {}, {class: "form-select"} %>
    </div>
     <%= link_to_remove_association "Remove", f, class:"btn btn-outline-secondary" %>
</div>
