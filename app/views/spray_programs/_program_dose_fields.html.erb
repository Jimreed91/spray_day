
<div class="nested-fields">
  <div class="field">
    <%= f.label :name %>
    <%= f.collection_select :product_id, Product.all.where(farm: current_user), :id, :name %>
    </div>
    <%= link_to_remove_association "Remove product", f %>
</div>
